export type Congressional = {
    fips: number;
    name: string;
}

export type State = {
    fips: number;
    name: string;
    stusab: string;
}

export type County = {
    fips: number;
    name: string;
}